# 更新记录

## 0.2.0-preview.1 — 换窗交接与来源识别（2026-09-21）

- 新增有界内存短期交接：同一已认证记忆身份/模型，可靠来源变化后交接最近 30 分钟内最多 3 条有效 AI 最终正文，不自动写长期记忆；重启清空。
- `ST_SOURCE_V1` 分开表达 `changed / same / unknown` 和 `handover_present`，工具续轮复用本轮判断；缺少窗口标识不表示用户不可信或消息被截断。
- 增加 `<网关公开模型ID>--auxiliary-no-memory` 辅助模型通道，用于标题/回复建议；不读写 ST 记忆、不干扰正式聊天会话，拒绝工具调用。
- 成功交付的最终正文才可缓存；思考、工具结果、未完成/超限回复及识别到凭证的正文不交接。缓存按有效策略与身份隔离，有过期清理及无正文的鉴权诊断接口。
- 使用者已在本机及 VPS 的 RikkaHub 路径验收；Orbis App、任意其他前端、重启恢复短期内容不在通过范围。本版不修改运行服务和数据库。
- 版本从 `0.1.0-preview.5` 升到 `0.2.0-preview.1`，保留预览状态，不冒称通用生产 1.0。详见 [发布说明](RELEASE-NOTES-0.2.0-preview.1.md)、[设置指南](docs/SHORT-TERM-MEMORY.md)与[验证范围](docs/UPDATE-VALIDATION.md)。

## 2026-09-18 内部控制通信修复（preview.5 后续补丁，版本不变）

- 经使用者反馈学校手机流量、接近 300K 上下文的实际对话顺畅，并授权发布，补回此前暂缓的 ControlClient 修复：默认内部控制客户端不再继承环境代理，防止本机/私网认证请求误走代理而失败或绕路。
- 仅改变内部控制客户端；模型上游代理策略不变，显式传入的客户端不被覆盖。标准安装使用回环 HTTP；自定义 HTTPS Control 如需私有 CA，应显式配置客户端，不能再依赖环境证书变量。
- 新增代理陷阱、无效代理配置、显式客户端保留及内外客户端隔离的合成回归。未合入私人网络、UDP 或防火墙脚本，不修改运行中的服务或记忆。
- 实测是使用者反馈，不是标准化测速，也不证明单一代码改动解决所有移动网络或长上下文延迟。验证范围见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

## 2026-09-18 运维说明补充（版本不变）

- 补充 [Linux 系统更新与 ST 服务重启](docs/LINUX-SERVICE-MAINTENANCE.md)：区分公版前台启动与自建受管服务，说明 Ubuntu `needrestart` 直接重启与额外离线收尾要求之间的冲突。
- 提供仅在适用部署使用的精确单服务例外、安全恢复检查和及时加载安全补丁的维护要求；不关闭全系统更新，不附私人恢复脚本，不新增自动恢复器。
- 使用者已确认既有 VPS 对话恢复；本次 Git 仅更新运维资料和源码清单，不修改记忆运行时代码，也不合入此前暂缓的网络/延迟改动。

## 0.1.0-preview.5 — 常用入口在手边，完整能力按需打开

- 新增显式日常工具档：`/mcp?tool_profile=daily` 返回 7 个入口；默认 `/mcp` 仍为完整 44 项。`stbrain_tools` 按分类或操作名展开准确参数，`stbrain_manage` 执行原有操作，保留原权限与参数校验。
- 精简日常参数目录，工具箱说明随工具目录保留，帮助新窗口发现自写提醒、浮现开关、人称改写、整理与经验查询等能力。客户端需重连、刷新目录，从新回合使用。
- `stbrain_open` 排在日常目录首位，无必填参数。网关仅在结构判定的新对话第一轮、且本轮确有该入口时提供可忽略的轻提醒，遵守总注入开关。
- `recall_tool_guidance(view="experiences")` 补齐成功、部分成功等全部结果的经验读回，支持按经验编号或文本筛选，返回数量与截断标记；作者自报可信度仍与独立核验区分。
- 修复学习记忆称呼校对中，省略空 `preceding_context_summary` 导致确认稿与存入稿不一致的误拒。非空内容及其他绑定继续严格检查。
- 中文口语凭证拦截补齐“密码我设成了……”等表达；普通流程、变量名和存放位置仍可记录，真实秘密在持久化前拒绝。
- 工具调用已发送却未收到回包时，新增默认 300 秒的独立等待与安全收尾；保护仍在执行的操作，不自动重做工具或伪造成功。
- 本次源码发布排除仍待验收的网络与延迟改动。目录体积优化不代表固定 token 数或响应时间承诺。

详情见 [preview.5 发布说明](RELEASE-NOTES-preview.5.md)，验证范围见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

## 0.1.0-preview.4 — 称呼由作者指定，流程再简单一点

- 可选人称校对简化为“指明称呼对应谁、换成什么 → 预览 → 确认”。单处匹配自动定位，多处同名时由作者选择本次位置；人物名称可以是作者自己的标记。
- 版本、预览校验信息和确认稿由 ST 从已保存的预览取得。保留明确确认、原始草稿、单次存入回执与回滚依据；旧调用显式提交的信息仍逐项核对。
- 完善人称预览的字段路径和模块版本说明，减少准备参数时的歧义。字面替换可以用于当前对话、群聊或历史人物，身份说明来自作者本次声明。
- 工具目录名称和额外参数不匹配时，网关提供更清楚的继续方式；错误批次保持未执行，公开错误信息控制在安全范围内。
- 对齐统一工具卡修改入口及专用兼容入口的实际用法和目录帮助。完整 MCP 目录仍为 44 项。
- 保留既有自动浮现、AI 自写提醒、缓存布局、Windows/Linux 部署说明和安装向导。验收与测试范围见 [本版验证记录](docs/UPDATE-VALIDATION.md)。

详情见 [preview.4 发布说明](RELEASE-NOTES-preview.4.md)。

## 0.1.0-preview.3 — 提醒由作者写，日常查改更顺手

- 平台说明更正（2026-09-13）：明确 Windows 与 Linux 的 ST 服务均已实际验收，Linux 已验环境为 Ubuntu 24.04 x64 / CPython 3.12.3，私人阿里云 VPS 已运行。将 Linux 新安装向导脚本尚待补做的空白端到端测试单独列出；安装引导先确认 Windows 电脑、Linux 电脑或 VPS 等已有托管位置，再安排手机接入。本次仅更正文档，版本、业务代码与原有平台条件保持不变。
- 安装入口补充（2026-09-13）：新增交互式安装向导，准备独立 Python 环境、随机服务密钥、私有配置与连接资料；首次检查短暂启动三个本机服务，完成健康和工具目录检查后停止。下载中断可保留同一配置继续。Windows 与 Linux 各用一份完整哈希锁，首页提供新手入口与平台条件。此次补充安装工具，已验收的 L25 记忆运行时代码保持不变；验证范围见 [安装指南](docs/INSTALL.md)。
- 文档补充：面向具备相应网关与 MCP 能力的 LLM，以“回复前自动想起”开篇，前置两种接入方式与作者选择；新增模块地图、多脑协作合成示例、模块说明和自动浮现验证方法。保留作者的设计理念、人称与主动整理原文，并明确实际接口、AI 归纳行动与 ST 记忆服务的分工。本次仅更新文档，代码版本及既有验收范围保持不变。
- 首页开篇采用昕昕新提供的四段设计理念：“直觉与想起”“连续与定义”“独特的自己”“接入与能力”；后接原有情景、浮现机制、架构与用法。此次仅为作者文案更新。
- 文档补充：将“AI 写下与设置的记忆，在回复前随语境自动浮现”前置到首页，以使用 ST 的 AI 为对话主体，补充日常情景、作者选择和记忆回到对话的架构说明；明确网关自动浮现与 MCP 主动查询的接入区别，以及分模块或全局关闭自动浮现的入口。并列区分当前已可用的自动浮现、主动整理、版本回看与后续渐隐设计。本次文案更新保持 preview.3 的代码、功能与验证范围。
- 自写轻提醒、人称建议与自动注入开关在指南和工具目录中明确呈现；当前人称提醒前置到五个存入工具的说明，成功回执也附带提示。
- 普通情感、学习、工具与规划共用 `revise_memory`；`stbrain_open(view="recall")` 统一搜索和分页浏览。原文与作者字段可修订，历史版本保留。
- `simple-memory-v1` 中，模块一首次激活后官端直连与网关调用均可写普通记忆；模块一首次激活前普通模块只读。直连核心写入使用密码授权，已验证网关核心写入使用执行绑定。
- 情感类型省略时未分类，普通记忆来源和可信度省略时未标注；规划父项可选。工具卡可填写 MCP 服务大名和中文场景标签，可信度由作者填写 0–100，有效期可选。
- 新配置模板选择 `tail-context-v2` 缓存布局，稳定帧前置、动态记忆置于本次消息尾部；既有配置省略变量时保留兼容路径。
- 提供隐藏输入的模块一密码哈希配置助手；密码与私有配置保存在源码外。
- 网关工具参数失败增加安全工具名、公开字段与校验类型，整批验证和原生 SSE 错误语义保持；历史中断原因与客户端长期展示分别核实。
- 当前简化与兼容目录均为 44 项，简化目录提供 `authorize_self_model` 并收起专用工具卡修改名；旧调用继续兼容。
- 重写公开指南、工具目录、缓存与组件说明；并列呈现人称选择、主动整理和 AI 自写提醒三个理念。

详情见 [preview.3 发布说明](RELEASE-NOTES-preview.3.md)，实际验证见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

## 0.1.0-preview.2 — 接入与资料管理改进

- 提供 `stiller_ops.py` 单终端检查、三组件启动监管、离线备份校验、新目录恢复与选表 JSONL 导出。
- 复用严格配置与执行绑定，备份明确要求停止全部写入者；导出结果按私有数据保护。
- 改善情感检索的查询特征复用，并为显式查询加入有限词法表达回退；自动注入评分与原文披露边界保持原规则。
- 重排首页，增加星空封面与自动注入情景，将完整哲学、工具和底层指南分别保留在 `docs/`。
- 将群讨论后的记忆渐隐方案写入设计文档；普通记忆渐隐尚未实施。
- 本次验证范围和局限见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

## 0.1.0-preview.1 — 源码开发预览

- 采用完整 PolyForm Noncommercial 1.0.0 标准许可，公开署名为昕昕 & 阿止。
- 补齐商业授权申请、贡献、隐私、安全、第三方权利与源码来源说明。
- 提供单独的源码归档清单检查器及测试，保留严格生产门禁。
- 收录冻结的 ST runtime、43 项 MCP 工具、控制面和兼容网关源码。
- 保留默认 legacy 缓存布局，提供主动选择的 anchored-v1 实验布局。
- 提供独立配置示例、三个服务的启动入口和已验收平台依赖哈希锁。

这是面向学习、审查和新环境测试的源码预览。完整测试与模拟启动结果见
[源码状态](source-release-state.json)，生产与真实客户端验收另行开展。
