# 源码来源与审查范围

对应发布：`0.1.0-preview.5`，ST 源码开发预览。公开项目署名：**昕昕 & 阿止**。

## 本版增量来源

本版以公开提交 `6c2b978e708385f5e7e4dd8fcd4a120bf80ca742`（preview.4）为基线，合入本机与真实客户端已试用的 L31–L36 累积功能修订：七入口分类工具箱、能力常驻说明、人称确认后存入、完整工具经验读回、首轮可选提醒及口语凭证拦截。206 份运行、Schema 和合成测试文件来自已冻结的 L36 源码；公开组件文档和已发布的跨 Python AST 测试保留并分别审查。

preview.5 首次发布时按作者选择暂缓了 Control 内部代理继承调整。2026-09-18 使用者反馈学校手机流量及接近 300K 上下文的对话顺畅，并授权补发；当前 `rikkahub_gateway/server.py` 已恢复冻结 L36 的 `ControlClient(... trust_env=False)` 构造行为，原 206 份来源文件现均与冻结来源逐字一致。另新增独立的内部代理回归测试，不混入原 206 份来源统计。首轮提醒及此前已验收的工具回包收尾逻辑保持。

私有部署、Tailscale/UDP、防火墙及网络排障脚本不属于公开包。本版不是运行中 L36 部署目录的逐字全量镜像；使用者反馈也不等于对所有网络、移动网络或长上下文响应时间作保证。新增回归和本次验收范围见 [验证记录](docs/UPDATE-VALIDATION.md)。

作者定稿理念、缓存 v2、安装向导与平台依赖锁延续公开基线。发布工作使用独立候选目录，产品源码与包装脚本、公开说明分别核对。运行数据库、私有配置、聊天和附件属于使用者数据，留在发布范围之外。preview.4 的验证记录保存在 [历史记录](docs/UPDATE-VALIDATION-preview.4.md)。

本版文件集合、源文件映射、哈希与检查结果以 `SOURCE-PACKAGE-MANIFEST.json`、发布状态文件和 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md) 为准。源码回归、封装检查、实际部署和手机体验分别记录；文档中的工具数量也与测试用例数量分别统计。

`preview.2` 曾从公开提交 `a1abd690d9b25c5a73ec78e15e8720e4beaf4ec9` 的归档建立候选，增加运维入口并改进检索及说明。下文的 120 文件一致性与旧网关哈希属于更早预览版的来源记录，供追溯使用；当前文件可能已经修订，应以本版清单核对。

## 前一预览版的基线来源

前一预览版使用 `cache-preview-frozen-20260908` 基线中明确列入允许清单的 ST Python 实现、测试和 JSON schema，再加入公开部署、依赖、授权及发布检查文件。核心目录为 `runtime/`、`mcp_server/`、`rikkahub_gateway/`、`schemas/` 和 `tests/`。

此次来源复核逐一比较基线清单的 **120 个文件**与公开候选内容，公开 SHA-256 全部吻合。清单区分原始源哈希与公开哈希：涉及私人身份的少量测试标签被等长度合成标签替代，不以删除安全用例来获得通过。核心网关 `rikkahub_gateway/server.py` 基线 SHA-256 为：

```text
6dfc7b3fc5178587ce19e0e953996d414c85abb929aba01d5f0494edf86a4c8a
```

这说明当时公开内容对应其已验收的冻结基线，不证明所有历史创作过程或每行代码的权利来源。当前分发树的完整性以本版源码包清单及检查器结果为准；新文档文件数与这 120 个历史基线文件分别核对。

## 第三方与 RikkaHub 的边界

本包不 vendor Python 依赖，不附带 venv、第三方 wheel 或 site-packages。依赖由部署者按声明另行安装，独立适用自己的许可证，见 [第三方提示](THIRD_PARTY_NOTICES.md)。

以下客户端源码来源复核是前一预览版的历史记录；本版增量检查范围以当前清单与验证记录为准。

当时对所审公开候选的文件清单检查未发现 Kotlin、Java、APK、JAR、AAR、wheel、本机动态库、可执行程序或数据库文件。该轮复核阅读了 ST 网关、工具执行适配和 MCP 入口，并检索源码中的 copyright、SPDX、移植、复制和改编来源声明：未发现标明复制 RikkaHub 实现的代码声明，也未发现原生客户端文件。源码中确有关于客户端历史序列化和工具续轮行为的兼容性处理。

这些历史证据支持的结论是：**当时的包是单独的 ST 服务端源码包，不包含本项目另行开发的 Android 前端；接口兼容不能等同于复制客户端实现，也不能反过来证明绝无借用。** 未进行与所有上游项目的逐行相似度比较，所审候选也不包含完整历史 Git 证据。不能据此宣称经过完整 clean-room 认证或全历史版权审计。

RikkaHub 所参考原生修订的 [许可证为 GNU AGPL v3](https://raw.githubusercontent.com/rikkahub/rikkahub/12ee935e0b0063dc3194d5145248795e071393cc/LICENSE)。如果未来要把其代码或改编作品纳入分发，必须另行核对并履行相应义务，不能因 ST 选择 PolyForm 就把上游代码改成商业受限许可。仅用不同进程或 HTTP 接口也不是针对所有衍生作品问题的法律保证。

## 权利与后续贡献

根目录 [LICENSE](LICENSE) 是此次发布采用的正式标准许可，仅授予许可方有权授予的权利，不覆盖第三方独立权利，不转让用户记忆或生活内容。第三方发现、未来新增依赖、外部贡献与商业交付都需分别审查。

未来贡献按 [CONTRIBUTING.md](CONTRIBUTING.md) 明确来源和授权；普通合并不自动授予商业再授权。若有人发现遗漏出处或不兼容的借用，请用合成/非敏感证据联系维护者。维护者应据实更正、补通知或移除无权分发的部分，而不是把未识别的来源当成零风险。

以上为本次实际可核对的来源说明和局限，不是法院认定、律师法律意见或对全球全部权利的无条件保证。源码包验收通过也不代替生产、真实客户端或节费验收。
