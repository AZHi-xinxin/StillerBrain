# 0.1.0-preview.5 · 常用入口在手边，完整能力按需打开

这一版汇集前后两轮的核心记忆与 MCP 更新。AI 可以从少量常用入口开始，需要自写提醒、整理记忆或查看工具经验时，再自行打开分类工具箱。

## 这次更新了什么

- **日常目录更轻。** 显式选择日常档后，常驻目录保留 7 个入口，具体操作的完整参数按需读取。当前 `simple-memory-v1` 分类工具箱可发现 45 项操作，包含常用能力和兼容操作；旧完整目录仍为 44 项。
- **换窗后仍看得见工具箱。** 分类入口的说明直接随 MCP 目录提供，指出自写提示词、安全阀、浮现开关、人称改写、归纳整理和经验查询等用途。是否查目录、是否使用，由模型按需要决定。
- **新对话更容易找到 ST。** `stbrain_open` 排在日常目录首位，无必填参数。经过 ST 网关的新对话第一轮，如果本轮目录确实提供这个工具，且总注入开关允许，会收到一条可忽略的入口提醒。它不会强制调用，也不会每轮重复。
- **成功经验也能读回。** 工具经验新增 `experiences` 视图，覆盖成功、部分成功及其他结果；支持按经验编号或文本筛选，并报告总数和是否截断。作者自报仍保留其来源与可信度标记。
- **学习记忆称呼校对顺利衔接存入。** 修复确认后因内部补齐空字段而误判稿件不一致的问题。作者可以用标题、摘要、正文三字段草稿完成预览、确认和统一存入，无需补隐藏空字段。
- **口语里的凭证同样受保护。** 补齐“密码我设成了……”等中文写法的拦截；普通轮换流程、变量名和存放位置仍可记录。
- **工具回包等待可以安全收尾。** 工具调用成功发出后独立计时，默认等待 300 秒。超时尝试关闭旧等待；操作仍在运行或控制面未确认关闭时继续保护现场，不自动重做操作，也不把未知结果说成成功。

## 升级后怎样使用

1. 由部署者按现有维护流程升级，保留自己的私有配置、记忆数据和备份。
2. 想使用日常档，在客户端将 ST MCP 地址末尾的 `/mcp` 改为 `/mcp?tool_profile=daily`。已有其他查询参数时追加 `&tool_profile=daily`，已有 `tool_profile` 时修改原值；原认证配置保持。
3. 重连并刷新 MCP 工具目录，从下一条新消息开始。应看到 `stbrain_open`、`remember_memory`、`remember_tool_guidance`、`revise_memory`、`advance_plan`、`stbrain_tools`、`stbrain_manage`。
4. 需要低频功能时，用 `stbrain_tools` 查看分类，或用 `stbrain_tools(action="操作名")` 读参数，再交给 `stbrain_manage`。已知参数可以直接执行，无需每次先读说明。

默认 `/mcp` 仍返回完整 44 项目录，升级不会自动切换。日常档是目录呈现方式，访问权限仍由原服务核验；关闭客户端整个 ST MCP 或禁用工具箱入口后，模型不能自行恢复客户端许可。旧窗口里已有的工具说明和回执也不会因升级消失。

调用例子、成功经验查询和称呼校对见 [TOOLS](docs/TOOLS.md)，完整连接方法见 [GUIDE](docs/GUIDE.md)。网关身份由服务端执行绑定验证；普通读写和核心自我修订的授权区别见 [模块一什么时候需要密码](docs/GUIDE.md#模块一什么时候需要密码)。

## 范围与验证

原有自动浮现、作者的人称选择、自写提醒、记忆版本和模块权限继续保留。本版是源码开发预览；本轮源码检查、回归测试与本机/VPS 使用验收分别记录在 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

网络与长上下文延迟改动仍待真实场景验收，本次源码发布不包含该部分。目录缩小描述的是发送的工具说明体积，不代表固定 token 数或任何响应时间承诺。
