# 0.2.0-preview.1 · 换窗之后，接住刚才的话

本次从 0.1 系列进入 0.2 功能预览系列，主要新增短期记忆与来源切换识别。已有长期记忆、工具箱与授权边界保留，不需要换模型供应商，不新增模型总结调用。

## 三个变化

1. **临时记住刚才的回复。** 在同一已认证身份与模型下，最多保留最近 3 条有效 AI 最终正文，单条有效期 30 分钟；可靠换窗时作为原文参考交接，不要求模型复述，不自动写进长期记忆。
2. **分清“换了窗”和“有东西可交接”。** `changed`、`same`、`unknown` 单独表示来源判断；`handover_present` 表示本轮是否真的带了交接块。首轮、过期或来源不足不伪装成“没换窗”。
3. **标题生成不再搅动正式对话。** 给快速模型选择同一公开模型名加 `--auxiliary-no-memory` 后缀的目录项；正式聊天继续用原模型。辅助请求不使用 ST 记忆、不打开正式会话，也不接收工具调用。

## 升级后要做什么

- 按既有安装的受控维护流程备份、排空当前工具链、更新完整源码并重启对应组件，不覆盖正在运行的文件，不替换私有配置和记忆库。见 [维护指南](docs/OPERATIONS.md)。
- 使用 `tail-context-v2` 并开启 `STBRAIN_GATEWAY_SHORT_TERM=1`；总浮现与情感浮现策略也需允许。代码从环境启动时短期开关省略默认为开启，不能只依赖旧配置没有这个变量来关闭。
- 在客户端刷新模型列表；正式聊天选原模型，标题/回复建议等快速任务选辅助模型。辅助项不是另一个供应商模型，只是 ST 的隔离路由，仍会产生正常上游调用费用。
- 前端须传稳定会话标识；不要给所有窗口填同一个静态值。详细头字段、异常原因和合成测试提示词见 [短期记忆指南](docs/SHORT-TERM-MEMORY.md)。

## 已验收与没有承诺的事

作者的 RikkaHub 实例换窗来源识别、零工具/零长期记忆读写的接句交接已验收；同窗下一轮没有重复交接。本机与 VPS 部署的自动化结果属于先前运行包记录，本次公开候选的重新测试另见 [验证记录](docs/UPDATE-VALIDATION.md)。

Orbis App 仍待来源标识适配，不能因服务端升级就声称所有前端互通；这不是屏幕焦点监视、完整聊天记录同步、持久记忆或跨身份共享。重启清空、超时失效；模型是否自然理解交接，仍取决于模型与客户端。群聊与私聊等不同受众不应共用可互相交接的身份/模型范围。

许可仍为 PolyForm Noncommercial 1.0.0；非商业源码开发预览，不更改既有许可，也不宣称完成全平台生产认证。
