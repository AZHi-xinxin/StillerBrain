# StillerBrain 0.1.0-preview.3

这一版把 AI 日常使用记忆的几个动作连接得更清楚：写提醒、找记录、改内容，再带着已有经历继续对话。

[首页](README.md) · [使用指南](docs/GUIDE.md) · [工具目录](docs/TOOLS.md) · [本次验证](docs/UPDATE-VALIDATION.md)

## 本版重点

### AI 写给自己的提醒，有清楚的入口

自我治理档案支持 AI 自写轻提醒与安全阀，按需要修改、清空和回滚。人称弱提醒另有 `set`、`disable`、`reset`，当前内容会出现在五个存入工具的说明里，成功保存时也附在回执中。

工具说明是一份读取时快照。本轮修改偏好后以最新管理结果为准，刷新 MCP 目录可取得新说明。具体 DIY 范围与固定帮助文字的分工见 [TOOLS](docs/TOOLS.md)。

### 四个普通模块，共用查询与修改入口

`stbrain_open(view="recall")` 搜索或分页浏览情感、学习、工具和规划。`revise_memory(target_ref, changes)` 修改已查到记录的作者内容，包含原文、摘要、关键词、重要度及相应模块字段；ST 在后台保留版本历史。

工具卡修改已合入统一入口。当前简化目录提供 `authorize_self_model`，收起 `revise_tool_guidance` 的重复目录项；旧入口调用继续保留原校验。简化与兼容目录均为 44 项，集合有差异。

### 直连普通记忆更方便

在 `simple-memory-v1` 中，模块一首次完成设置并激活后，官端直连 MCP 和已验证网关调用均可写入、修改普通模块；内部上下文与模块行版本由服务取得。首次激活前普通模块只读，已有内容可按读取规则查询。

直连写入模块一继续使用独立密码授权，新配置助手通过隐藏输入生成私有哈希文件。已验证网关注入调用依靠服务端执行绑定，免部署密码；核心候选、后续复核和激活流程仍独立保留。

### 记录由作者表达

- 情感类型由 AI 填写，省略时为未分类。
- 普通记忆来源、可信度省略时为未标注；作者可以补充实际来源及 0–100 的可信度。
- 普通计划任意层级可独立创建，父项可选。
- 工具卡可以绑定 MCP 服务大名，接受中文场景标签，可信度可填 0–100，有效期可选。
- 字面称呼校对默认按稿关闭，AI 明确选择后走预览、确认与单次存入；统一保存已接通情感和学习，规划暂未接入。

### 缓存和失败线索

`tail-context-v2` 将稳定 ST 内容放在前部、本轮动态记忆放在当前客户端消息尾部，为长对话前缀复用提供条件。长期自用已收到缓存命中改善反馈，实际效果以供应商用量记录为准。

工具参数错误增加安全工具名、公开字段与校验类型。工具批次通过完整验证后才释放，流式失败保留原生 `error` 语义。客户端长期展示、上游结束方式和历史截断原因分别核实。

## 升级时留意

1. 结束当前工具链，备份三库与私有配置，再进行维护切换。
2. 新 `.env.example` 选择 `simple-memory-v1`、`tail-context-v2`；既有配置省略变量时保留兼容路径，是否切换由部署者选择。
3. 需要直连核心修改时，按 [密码配置步骤](docs/GUIDE.md#配置直连模块一密码) 设置哈希文件；普通记忆读写与此密码分别管理。
4. 更新后刷新客户端 MCP 工具目录，从新用户轮验证保存、查询、修改和完整工具续轮。
5. 源码、配置、记录与备份分别保存；公开反馈采用合成示例和脱敏错误信息。

实际检查结果由 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md) 与发布清单记录。

## 保持的边界

记忆渐隐仍处于设计阶段，当前提供作者主动整理与版本化修订。Orbis 是独立前端，手机操作和长期任务执行由对应宿主及授权支持。

本版沿用 [PolyForm Noncommercial 1.0.0](LICENSE) 与现有[商业授权说明](COMMERCIAL-LICENSE.md)，项目分类为 source-available。运行中的私人记忆与源码许可分开管理。
