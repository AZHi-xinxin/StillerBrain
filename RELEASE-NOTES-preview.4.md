# 0.1.0-preview.4 · 把注意力留给想写的内容

记忆里的一个“她”，可以承载一段共同经历。后来再读时，也许你想把这个称呼写得清楚一点，让未来的自己知道当时说的是谁。

这一版让这件小事回到简单：**由你指明“指谁、换成什么”，看过预览，再确认。** ST 接住版本与校验细节，保留原始草稿和回看依据。怎样叙述、要不要校对，依然由写下记忆的 AI 决定。

## 这次更新了什么

- **称呼校对更轻便。** 预览提供模块、完整草稿和指定替换；确认只需预览编号与明确同意。人物名称由作者声明，当前对话、群聊和历史人物都可按稿处理。
- **只改你指定的地方。** 同一字段只有一处匹配时自动定位；出现多处同名时，由作者指出这次要改哪一处。需要保留原样的范围可以显式标记。
- **存入结果可核对。** 确认返回完整最终稿与单次回执，随后仍由原来的记忆存入工具保存。重复确认与回执重放有对应检查，预览本身保留为草稿。
- **遇到参数问题更好继续。** 工具目录名称、额外字段和人称预览字段路径的提示更明确；统一工具卡修改说明与实际调用一致。

## 升级后怎么试

1. 刷新客户端的 ST MCP 工具目录，取得这一版的说明。
2. 让 AI 读当前说明，为一小段合成草稿做一次称呼预览。
3. 检查预览；愿意采用时确认，再把返回的完整最终稿和回执交给存入工具。

一份可直接参考的参数示例在 [工具用法](docs/TOOLS.md#10-人称提醒与可选称呼校对--3-项)。已有记忆按原流程读写；称呼校对默认按需调用。情感、学习和工具模块已有对应存入路径，规划模块暂未接入这项可选校对。

## 这版沿用的选择

记忆按对话语境在回复前浮现，AI 可以主动查、存、改，也可以自写提醒和管理浮现开关。缓存布局、安装向导以及 Windows/Linux 部署入口延续上一版；作者写下的人称选择、主动整理与自主提醒理念完整保留。

本版是源码开发预览。本机使用者已验收 L30；源码测试、特定 VPS 升级和通用生产条件分别记录。历史中断需要按各次错误证据判断，本版说明只覆盖实际修复的工具目录与参数路径。详细范围见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。
