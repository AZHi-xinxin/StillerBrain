# 源码来源与审查范围

对应发布：`0.1.0-preview.2`，ST 源码开发预览。公开项目署名：**昕昕 & 阿止**。

本次增量从公开提交 `a1abd690d9b25c5a73ec78e15e8720e4beaf4ec9` 的归档建立独立候选，新增运维入口、合成测试及说明，修改情感检索和首页文档。未从用户运行目录打包数据库、配置、聊天或附件；没有引入新第三方依赖。增量验证见 [UPDATE-VALIDATION](docs/UPDATE-VALIDATION.md)。

下文 120 文件的逐项一致性是前一预览版的来源复核记录，不代表修改后的所有文件仍与旧基线逐字相同。本版完整文件集合与哈希以当前 `SOURCE-PACKAGE-MANIFEST.json` 为准。

## 前一预览版的基线来源

本版使用 `cache-preview-frozen-20260908` 基线中明确列入允许清单的 ST Python 实现、测试和 JSON schema，再加入公开部署、依赖、授权及发布检查文件。核心目录为 `runtime/`、`mcp_server/`、`rikkahub_gateway/`、`schemas/` 和 `tests/`。

此次来源复核逐一比较基线清单的 **120 个文件**与公开候选内容，公开 SHA-256 全部吻合。清单区分原始源哈希与公开哈希：涉及私人身份的少量测试标签被等长度合成标签替代，不以删除安全用例来获得通过。核心网关 `rikkahub_gateway/server.py` 基线 SHA-256 为：

```text
6dfc7b3fc5178587ce19e0e953996d414c85abb929aba01d5f0494edf86a4c8a
```

这说明本次公开内容对应已验收的冻结基线，不证明所有历史创作过程或每行代码的权利来源。最终分发树的完整性以本版源码包清单及检查器结果为准；不要把新文档文件数和 120 个基线文件数混为一谈。

## 第三方与 RikkaHub 的边界

本包不 vendor Python 依赖，不附带 venv、第三方 wheel 或 site-packages。依赖由部署者按声明另行安装，独立适用自己的许可证，见 [第三方提示](THIRD_PARTY_NOTICES.md)。

对所审公开候选的文件清单检查未发现 Kotlin、Java、APK、JAR、AAR、wheel、本机动态库、可执行程序或数据库文件。此次复核阅读了 ST 网关、工具执行适配和 MCP 入口，并检索源码中的 copyright、SPDX、移植、复制和改编来源声明：未发现标明复制 RikkaHub 实现的代码声明，也未发现原生客户端文件。源码中确有关于客户端历史序列化和工具续轮行为的兼容性处理。

这些证据支持的结论是：**当前包是单独的 ST 服务端源码包，不包含本项目另行开发的 Android 前端；接口兼容不能等同于复制客户端实现，也不能反过来证明绝无借用。** 未进行与所有上游项目的逐行相似度比较，所审候选也不包含完整历史 Git 证据。不能据此宣称经过完整 clean-room 认证或全历史版权审计。

RikkaHub 所参考原生修订的 [许可证为 GNU AGPL v3](https://raw.githubusercontent.com/rikkahub/rikkahub/12ee935e0b0063dc3194d5145248795e071393cc/LICENSE)。如果未来要把其代码或改编作品纳入分发，必须另行核对并履行相应义务，不能因 ST 选择 PolyForm 就把上游代码改成商业受限许可。仅用不同进程或 HTTP 接口也不是针对所有衍生作品问题的法律保证。

## 权利与后续贡献

根目录 [LICENSE](LICENSE) 是此次发布采用的正式标准许可，仅授予许可方有权授予的权利，不覆盖第三方独立权利，不转让用户记忆或生活内容。第三方发现、未来新增依赖、外部贡献与商业交付都需分别审查。

未来贡献按 [CONTRIBUTING.md](CONTRIBUTING.md) 明确来源和授权；普通合并不自动授予商业再授权。若有人发现遗漏出处或不兼容的借用，请用合成/非敏感证据联系维护者。维护者应据实更正、补通知或移除无权分发的部分，而不是把未识别的来源当成零风险。

以上为本次实际可核对的来源说明和局限，不是法院认定、律师法律意见或对全球全部权利的无条件保证。源码包验收通过也不代替生产、真实客户端或节费验收。
