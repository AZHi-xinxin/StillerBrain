# 安全说明

本项目处理敏感记忆和模型凭证。`0.1.0-preview.1` 是源码开发预览，不是生产安全认证或经过独立渗透测试的多租户托管服务。

## 部署边界

- 开发部署默认只监听回环地址；不要把 MCP、控制面或网关直接公开到互联网。控制面不是 AI 工具入口，也不应作为公开服务。
- MCP、宿主、人类、网关 Token、唤醒密钥和上游 Key 使用独立值；配置、凭证、运行日志和三个数据库都放在源码仓库外。
- 三个组件保持一致的受控部署 epoch 与执行绑定配置；不要关闭跨轮校验、身份或凭证保护来“先跑起来”。
- 一个实例服务一个明确的 owner/model 配置；不得混用不同用户或身份的数据路径。
- 日常记忆一次保存不等于放弃来源、跨窗口、版本和真实执行回执保护。工具卡、计划和记忆不能授予账号操作权限。
- 缓存优化不能放宽这些保护。历史变更或工具续轮失败应明确处理；模型声称成功不是现实成功证明，结果未知的写入不能盲目重放。
- 备份应包含一致的主库和附属库。不要忽略 SQLite WAL 或把正在使用的单个数据库文件当成完整备份；升级前使用经验证的备份流程。

## 报告漏洞

目前本文件不宣称某个私密漏洞报告入口已经启用，也不提供私人邮箱。如果实际仓库已经启用 GitHub 的私密漏洞报告入口，可以使用该入口；否则先在仓库 Issue 提交**不含漏洞细节和秘密**的联系请求，等待维护者通过可核实的仓库回复确认私密渠道。

不要在公开 Issue、PR、聊天或合同申请中提交凭证、个人记忆、数据库和完整请求。尽量使用合成复现。确认私密渠道前不要发送非公开细节。

若凭证误公开，应立即在供应商或宿主侧撤销、轮换，并检查其使用情况；删除 Issue、文件或 Git 历史不保证副本消失。

## 版本与报告真实性

本版没有承诺固定的安全响应时限或生产支持周期。请报告具体版本和复现范围。自动扫描、已知漏洞查询和测试通过都不是“没有漏洞”的证明。

公开前检查整个新仓库历史和实际打包文件，不从个人工作区根目录直接 `git add .`。只有源码包验收通过，不意味着生产或真实客户端验收已完成；保持两种状态分开。数据处理边界见 [PRIVACY.md](PRIVACY.md)。
